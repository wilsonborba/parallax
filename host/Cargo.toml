[package]
name = "host"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
x11 = { version = "2.21", features = ["xlib"] }
ffmpeg-next = { version = "8", default-features = false, features = [
  "codec",
  "format",
  "software-scaling"
] }
